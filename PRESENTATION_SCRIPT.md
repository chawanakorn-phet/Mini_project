# สคริปนำเสนอ — Olist Data Warehouse (แบ่งพูด 4 คน)

ผู้พูด: **เน็ท, จ๋อมแจ๋ม, ปิ๊ก, เวฟ** — สลับกันตามหัวข้อด้านล่าง เวลารวมประมาณ 10-12 นาที

---

## (เน็ท) 👤 คนที่ 1: Introduction, Domain, Objectives & Dataset Selection

> "สวัสดีครับ/ค่ะ อาจารย์และเพื่อนๆ ทุกคน พวกเราขอนำเสนอโครงงานวิชา Data Warehousing ในหัวข้อ **Olist E-Commerce Analytics** ระบบวิเคราะห์ข้อมูลตลาดกลางอีคอมเมิร์ซของบราซิลครับ/ค่ะ
>
> ข้อมูลที่นำมาศึกษาคือ **ธุรกรรมจริง** ของแพลตฟอร์ม Olist ซึ่งเป็นมาร์เก็ตเพลสที่เชื่อมผู้ขายรายย่อยกับลูกค้าทั่วประเทศบราซิล ครอบคลุมช่วงปี 2016-2018 มีทั้งหมด **9 ตาราง รวมกว่า 1.55 ล้านแถว** ตั้งแต่ข้อมูลลูกค้า คำสั่งซื้อ การชำระเงิน สินค้า ผู้ขาย ไปจนถึงรีวิวหลังส่งของ
>
> เหตุผลที่เราเลือก Dataset นี้ — เพราะเป็น **ข้อมูลจริงที่เปิดเผยสาธารณะบน Kaggle** ไม่ใช่ข้อมูลจำลอง และมีจุดเด่นสำคัญคือ ตาราง `orders` เก็บ **timestamp 5 จุดตลอดวงจรออเดอร์** ตั้งแต่สั่งซื้อจนถึงได้รับสินค้า ทำให้วิเคราะห์เรื่องเวลาและการจัดส่งได้ลึกมาก
>
> วัตถุประสงค์ของโครงการแบ่งเป็น 2 ส่วนครับ/ค่ะ:
> 1. **เชิงธุรกิจ**: สร้าง Interactive Dashboard บน Streamlit ให้ผู้บริหารมาร์เก็ตเพลสเห็นภาพรวมยอดขาย พฤติกรรมลูกค้า ประสิทธิภาพการจัดส่ง และคุณภาพของผู้ขาย เพื่อใช้ตัดสินใจได้ทันที
> 2. **เชิงการเรียนรู้**: ฝึกออกแบบ Data Warehouse จาก ER Diagram ของระบบ OLTP ไปสู่ **Fact Constellation (Galaxy Schema)** และสร้าง ELT Pipeline ด้วย dbt + DuckDB ร่วมกันทั้งทีมผ่าน GitHub ครับ/ค่ะ"

---

## (จ๋อมแจ๋ม) 👤 คนที่ 2: Operational Database (OLTP) & ปัญหาที่ต้องแก้

> "ขอบคุณครับ/ค่ะ ในส่วนของฐานข้อมูลเชิงปฏิบัติการเดิม ข้อมูลถูกบันทึกแยกกระจายอยู่ในรูปแบบ CSV ทั้งหมด **9 ตาราง รวมกว่า 1,550,922 แถว** โดยแบ่งตามบทบาทได้ดังนี้ครับ/ค่ะ:

| ตาราง | บทบาท | จำนวนแถว |
|---|---|---:|
| `olist_orders_dataset` | หัวคำสั่งซื้อ + สถานะ + timestamp 5 จุดตลอดวงจร (สั่งซื้อ→อนุมัติ→ส่งขนส่ง→ถึงมือลูกค้า→วันสัญญา) | 99,441 |
| `olist_customers_dataset` | ลูกค้า 1 แถวต่อ 1 การสั่งซื้อ 1 ครั้ง + zip code ที่อยู่ | 99,441 |
| `olist_order_items_dataset` | รายบรรทัดสินค้าในคำสั่งซื้อ ราคา ค่าจัดส่ง | 112,650 |
| `olist_order_payments_dataset` | การชำระเงิน — 1 ออเดอร์จ่ายได้หลายรอบ หลายวิธี | 103,886 |
| `olist_order_reviews_dataset` | แบบสอบถามความพอใจหลังได้รับสินค้า คะแนน 1-5 | 99,224 |
| `olist_products_dataset` | แคตตาล็อกสินค้า หมวดหมู่ (ภาษาโปรตุเกส) ขนาด น้ำหนัก | 32,951 |
| `olist_sellers_dataset` | ผู้ขายและ zip code ที่ตั้ง | 3,095 |
| `olist_geolocation_dataset` | lookup zip code prefix → ละติจูด/ลองจิจูด/เมือง/รัฐ (ซ้ำหลายแถวต่อ 1 zip) | 1,000,163 |
| `product_category_name_translation` | แปลชื่อหมวดหมู่ โปรตุเกส → อังกฤษ | 71 |
>
> จะเห็นว่าศูนย์กลางของระบบคือ `orders` ซึ่งมี child เชื่อมอยู่ 3 ตารางที่ **grain ไม่เท่ากันเลย** — 1 บรรทัดสินค้า ≠ 1 รายการชำระเงิน ≠ 1 รีวิว นี่คือประเด็นที่จะกลายเป็นเหตุผลหลักตอนออกแบบ Data Warehouse
>
> ปัญหาของโครงสร้างแบบเดิมมี 2 เรื่องครับ/ค่ะ:
> 1. **JOIN ลึกเกินไป** — ถ้าผู้บริหารอยากรู้คำถามเชิงวิเคราะห์ เช่น *'ส่งช้าทำให้คะแนนรีวิวแย่ลงไหม'* ต้อง JOIN สดๆ ข้าม orders → order_items → order_reviews → products → geolocation ทุกครั้งที่ query ทำให้ทำงานหนักและช้า
> 2. **ที่อยู่กระจัดกระจาย** — ที่อยู่ลูกค้าอยู่ในตาราง `customers` ส่วนที่อยู่ผู้ขายอยู่ในตาราง `sellers` คนละที่กัน ทั้งที่ทั้งคู่ต้องอ้างอิง `geolocation` เหมือนกัน ทำให้เทียบ 'รัฐที่ซื้อ' กับ 'รัฐที่ขาย' บนมาตรฐานเดียวกันไม่ได้ และคำนวณระยะทางผู้ซื้อ-ผู้ขายไม่ได้เลยถ้าไม่ transform ก่อน
>
> ดังนั้นเราจึงต้อง Transform ข้อมูลนี้ให้เป็นคลังข้อมูลใหม่ ซึ่ง [ปิ๊ก] จะมาอธิบายว่าออกแบบเป็นคำถามธุรกิจและ Multidimensional Model อย่างไรครับ/ค่ะ"

---

## (ปิ๊ก) 👤 คนที่ 3: Business Questions & Galaxy Schema Deep Dive

> "ขอบคุณครับ/ค่ะ เราตั้งต้นจาก **Business Questions 15 ข้อ** จัดกลุ่มเป็น 8 หมวดตามกระบวนการธุรกิจ ทุกข้อระบุชัดเจนว่าตอบด้วย Fact ตารางไหน เดินผ่าน Dimension ใด วัดด้วย Measure อะไร ขอยกตัวอย่างทีละหมวดครับ/ค่ะ:
> - **กลุ่มยอดขายและสินค้า** — คำถามข้อ 1 *"หมวดหมู่สินค้าใดมียอดขายรวมและจำนวนสินค้าที่ขายสูงสุด"*: Fact = `fact_order_items`, Dimension = `dim_products` (category), Measure = `SUM(price)` และ `COUNT(*)`
> - **กลุ่มลูกค้า** — คำถามข้อ 8 *"ลูกค้ากลุ่มใดมีมูลค่าและความถี่ในการซื้อสูงสุดจาก RFM Analysis"*: Fact = `fact_order_items`, Dimension = `dim_customers` + `dim_date`, Measure = Recency `MAX(date)`, Frequency `COUNT(DISTINCT order_id)`, Monetary `SUM(price)` แล้วแบ่งกลุ่มด้วย `NTILE(5)`
> - **กลุ่มการจัดส่ง** — คำถามข้อ 10 *"ผู้ขายในรัฐหรือเมืองใดมีระยะเวลาเตรียมสินค้าเฉลี่ยสูงสุด"*: Fact = `fact_order_items`, Dimension = `dim_sellers` (state), Measure = `AVG(seller_processing_days)`
> - **กลุ่มรีวิว** — คำถามข้อ 13 *"คะแนนรีวิวแตกต่างกันอย่างไรระหว่างลูกค้าในแต่ละรัฐ"*: Fact = `fact_order_items` **+** `fact_order_reviews` (drill-across), Dimension = `dim_customers` + `dim_date`, Measure = `AVG(is_late_delivery)`, `AVG(buyer_seller_distance_km)`, `AVG(review_score)`
>
> จุดที่อยากกันสับสนล่วงหน้า — เวลาเราพูดถึง `price` เฉยๆ หมายถึง **คอลัมน์ดิบ** ที่เก็บอยู่ใน `fact_order_items` แต่ละแถว (ราคาต่อสินค้า 1 ชิ้น) แต่พอเป็น `SUM(price)` แบบในตัวอย่างข้อ 1 หมายถึง **การรวมคอลัมน์นั้นข้ามหลายแถว** เพื่อตอบคำถามที่ grain หยาบกว่า (เช่น รวมทั้งหมวดหมู่) ตัวคอลัมน์เหมือนกัน แต่ต่างกันที่ว่าตอนตอบคำถามต้อง aggregate ด้วยฟังก์ชันอะไรครับ/ค่ะ
>
> ### Multidimensional Data Model — 7 Dimensions
>
> ทุกตาราง Dimension ของเราจะมี **Surrogate Key** เป็นของตัวเอง คือรหัสใหม่ที่เราสร้างขึ้นเอง ไม่ได้ใช้รหัสจากระบบเดิมตรงๆ แต่ก็ยังเก็บรหัสเดิมไว้ในตารางด้วยเผื่อต้องย้อนกลับไปดู
>
> นอกจากนี้ทุก Dimension จะมีแถวพิเศษเรียกว่า **Unknown Member** ซึ่งมี key เป็น **-1** ไว้รองรับกรณีที่ข้อมูลใน Fact อ้างถึงค่าที่หาไม่เจอใน Dimension เช่น ออเดอร์ที่ไม่มีรีวิว หรือลูกค้าไม่มีที่อยู่ระบุ พอมีแถวนี้เผื่อไว้ แถวใน Fact ก็จะไม่หายไปตอนเรา join ครับ/ค่ะ:
>
> | Dimension | Primary Key | Attribute หลัก | หน้าที่ |
> |---|---|---|---|
> | `dim_date` | `date_key` | year, quarter, month, month_name, day_name, is_weekend | วิเคราะห์ตามเวลา — **Conformed ทั้ง 3 Fact · Role-play 5 บทบาทใน fact_order_items, 3 บทบาทใน fact_order_reviews** |
> | `dim_geography` | `geography_key` | region, state, city, zip_code_prefix, latitude, longitude | วิเคราะห์ตามภูมิศาสตร์ — **Conformed ทั้ง 3 Fact · Role-play 2 บทบาท (ที่อยู่ลูกค้า / ที่อยู่ผู้ขาย)** |
> | `dim_customers` | `customer_key` | customer_id, customer_unique_id, state, city | วิเคราะห์ลูกค้าและพฤติกรรมซื้อซ้ำ / RFM — **Conformed ทั้ง 3 Fact** |
> | `dim_sellers` | `seller_key` | seller_id, state, city, geography_key | วิเคราะห์ยอดขายและประสิทธิภาพผู้ขาย |
> | `dim_products` | `product_key` | category, size_band, photos_qty, weight_g | วิเคราะห์สินค้าและหมวดหมู่ |
> | `dim_order_status` | `order_status_key` | order_status, status_label, lifecycle_step, is_delivered | วิเคราะห์สถานะและวงจรคำสั่งซื้อ — **Conformed ทั้ง 3 Fact** |
> | `dim_payment_type` | `payment_type_key` | payment_type, payment_label, supports_instalments | วิเคราะห์วิธีการชำระเงิน |
>
> จุดที่เปลี่ยนมากที่สุดจากโมเดลเดิมคือ **`dim_geography`** — เดิมที่อยู่ลูกค้ากับผู้ขายกระจัดกระจายอยู่คนละตาราง เรายุบตาราง `geolocation` 1 ล้านแถวให้เหลือ **1 พิกัดต่อ 1 zip code** แล้วให้ทั้งลูกค้าและผู้ขายชี้มาที่ dimension เดียวกัน ผลคือคำนวณ **ระยะทางผู้ซื้อ-ผู้ขาย** ได้ (ใช้ตอบข้อ 13) ซึ่งโมเดลเดิมทำไม่ได้เลย
>
> ### Fact Tables — 3 ตาราง คนละ Grain (ยกมาจาก README §3.2 ตรงตัว)
>
> - **`fact_order_items`** — grain คือ 1 order line item (`order_id` + `order_item_id`) มี 112,650 แถว Base Measures ได้แก่ `price`, `freight_value`, `delivery_days`, `seller_processing_days`, `carrier_transit_days`, `delivery_delay_days`, `buyer_seller_distance_km`, `is_late_delivery`, `purchase_hour` — จัดเป็น **Additive:** price, freight_value / **Semi-Additive:** is_late_delivery / **Non-Additive:** delivery_days, seller_processing_days, carrier_transit_days, delivery_delay_days, buyer_seller_distance_km, purchase_hour ส่วน Calculated Measures คือ total_item_value, Average Price per Item, Freight % of Price, Late-Delivery Rate, MoM Revenue Growth %
> - **`fact_order_payments`** — grain คือ 1 payment transaction (`order_id` + `payment_sequential`) มี 103,886 แถว Base Measures ได้แก่ `payment_value`, `payment_installments` — จัดเป็น **Additive:** payment_value / **Non-Additive:** payment_installments ส่วน Calculated Measures คือ instalment_amount, Average Transaction Value, Split-Payment Rate
> - **`fact_order_reviews`** — grain คือ 1 review per order (`review_id` + `order_id`) มี 99,224 แถว Base Measures ได้แก่ `review_score`, `response_hours`, `has_comment` — จัดเป็น **Non-Additive:** review_score, response_hours / **Semi-Additive:** has_comment, is_positive, is_negative ส่วน Calculated Measures คือ Average Review Score, Comment Rate, comment_length
>
> ทำไม Measure แต่ละตัวถึงถูกจัดประเภทแบบนั้น (README §3.3): **Additive** อย่าง price, freight_value, total_item_value, payment_value รวมได้ตรงทุกมิติ (เวลา/สินค้า/ลูกค้า/ผู้ขาย) → SUM มีความหมายเป็นยอดรวมจริง **Semi-Additive** อย่าง is_late_delivery, has_comment, is_positive, is_negative (flag 0/1) นับ/รวมได้ภายในชุดที่กำหนด แต่ต้องระวัง grain ตอน join ข้าม fact ไม่งั้นนับซ้ำ ส่วน **Non-Additive** อย่าง delivery_days, seller_processing_days, carrier_transit_days, delivery_delay_days, buyer_seller_distance_km, purchase_hour, payment_installments, review_score, response_hours เป็นระยะเวลา/ระยะทาง/ค่าหมวดหมู่/ordinal scale ที่ SUM ข้ามแถวไม่มีความหมาย ใช้ได้แค่ AVG/MIN/MAX/GROUP BY เท่านั้นครับ/ค่ะ
>
> ### ทำไมต้องเป็น Galaxy Schema (Fact Constellation)
>
> เราพบว่ามี **3 business process ที่ grain ต่างกัน** ถ้ายัดรวมเป็น Fact เดียวแบบ Star Schema ตัวเลขจะซ้ำซ้อนทันที เช่น ถ้ารวม review เข้ากับ order_items รีวิว 1 อันจะถูกนับซ้ำตามจำนวนสินค้าในออเดอร์นั้น เราจึงแยกเป็น **3 Fact**: `fact_order_items`, `fact_order_payments`, `fact_order_reviews` และให้ทั้ง 3 ใช้ **Conformed Dimension ร่วมกัน** เช่น `dim_date`, `dim_customers`, `dim_geography`, `dim_order_status`
>
> จุดที่พิสูจน์ว่า Galaxy Schema **จำเป็นจริง** ไม่ใช่แค่ทำให้ดูซับซ้อน คือมี 3 คำถามที่ Fact เดียวตอบไม่ได้ ต้องทำ **Drill-Across** — รวมแต่ละ Fact ให้อยู่ grain เดียวกันก่อนแล้วค่อย join กันผ่าน conformed dimension:
> - ข้อ 6: มูลค่าตะกร้า (`fact_order_items`) เทียบกับยอดจ่ายจริงรวมงวดผ่อน (`fact_order_payments`) เชื่อมผ่าน `order_id`
> - ข้อ 12: คะแนนรีวิวเฉลี่ยรายหมวดหมู่สินค้า — เชื่อม `fact_order_reviews` กับ `fact_order_items` ผ่าน `dim_products`
> - ข้อ 13: ส่งช้ากับระยะทางกระทบคะแนนรีวิวไหม — เชื่อม 2 Fact ผ่าน `dim_customers` และ `dim_date`
>
> อีกจุดคือ `dim_date` ทำหน้าที่ **Role-Playing 5 บทบาท** ใน `fact_order_items` เดียว เพราะ 1 ออเดอร์มีวันที่ 5 จุด (สั่งซื้อ, อนุมัติ, ส่งขนส่ง, ถึงมือลูกค้า, วันที่สัญญาไว้) ตารางเดียวแต่ใช้ตอบได้หลายมุมมองเวลา
>
> ตอนสร้าง `fact_order_reviews` เราเจอบั๊กจริงระหว่างกระทบยอด — ตอนแรกคิดว่า grain คือ 1 review_id ต่อ 1 แถว แต่นับแล้วขาดไป 814 แถว สืบพบว่า Olist มีการใช้ **review_id ซ้ำข้ามหลายออเดอร์** (789 review_id ผูกกับ 1,412 ออเดอร์) เราจึงแก้ grain เป็นคู่ `(review_id, order_id)` ซึ่ง unique พอดี 99,224 แถว — เป็นตัวอย่างที่แสดงว่าเราตรวจสอบ grain ด้วยการกระทบยอดจริง ไม่ได้เดา และมี test `assert_fact_order_reviews_grain_is_unique.sql` ล็อกไว้กันไม่ให้กลับมาเป็นซ้ำอีก
>
> ### ELT Pipeline
>
> ใช้ **dbt + DuckDB**: Extract CSV 9 ไฟล์ → Staging layer (`stg_*`) ทำสำเนา 1:1 พร้อม audit timestamp → Warehouse layer (`dim_*`/`fact_*`) ที่ทำ cleaning จริง เช่น สร้าง Surrogate Key, ใส่ **Unknown Member (key = -1)** กัน fact หายเวลาอ้างค่าที่ไม่มี, standardize ชื่อเมืองที่สะกดไม่ตรงกัน, และสร้าง `dim_date` ปฏิทินขึ้นเองเพราะต้นทางไม่มีตารางวันที่
>
> ทดสอบคุณภาพด้วย `dbt test` ทั้งหมด **87 เทสต์ ผ่าน 100%** ครอบคลุม unique/not_null บนทุก key, relationships 20 จุดพิสูจน์ว่า FK ทุกเส้น resolve เข้า dimension จริง และ singular test ตรวจว่ายอดเงินตรงกับ source ทุกสตางค์ครับ/ค่ะ ต่อไป [เวฟ] จะพาไปดู Dashboard จริงครับ/ค่ะ"

---

## (เวฟ) 👤 คนที่ 4: สคริปสำหรับ "สไลด์ 15 — Web App" (Live Dashboard Demo)

> เนื้อหานี้ต่อจากสไลด์ 14 (Data Warehouse Database — 16 ตารางทั้งหมด, Staging 9, Dimension+Fact 7+3, dbt test ผ่านทั้งหมด) สไลด์ 15 ในเดคไม่มีเนื้อหาละเอียดเพราะ **ตั้งใจเปิดเว็บจริงโชว์อาจารย์แทนสไลด์** — เวฟใช้ script นี้พูดคลอไปกับตอนคลิกเว็บสดๆ ได้เลย ไม่ต้องท่องสไลด์

> "จากสไลด์ที่แล้วที่เห็นว่า Data Warehouse ของเราผ่าน dbt test ครบทุกจุด ต่อไปนี้จะพาไปดูของจริงบนเว็บครับ/ค่ะ ส่วนนี้ตั้งใจไม่ทำเป็นสไลด์นิ่งๆ เพราะอยากให้อาจารย์เห็นว่าตัวกรองใช้งานได้จริง ไม่ใช่ภาพนิ่ง"

🎬 *(เปิด Dashboard จริงที่ deploy บน Streamlit Cloud — ออกแบบเป็น **ภาษาไทยทั้งหมด** สำหรับผู้ชมทั่วไป ป้ายกำกับ ชื่อหมวดหมู่สินค้า ภูมิภาค วิธีชำระเงิน แปลไทยหมด ไม่มีชื่อคอลัมน์ดิบ ลองเปลี่ยน Filter ช่วงวันที่ก่อน 1 ครั้งเพื่อโชว์ว่าตัวกรองไม่พัง จากนั้นไล่ตามหมวด KPI ที่เพิ่งพูดถึงในสไลด์ 3 ทีละหมวด)*

> "ต่อไปนี้จะไล่ทีละแท็บ ทุกกราฟจะอธิบาย 4 อย่าง: **ดึงข้อมูลจากไหน → กราฟบอกอะไร → ใช้กราฟแบบไหนและทำไม → ตอบคำถามข้อไหน** ครับ/ค่ะ"

---

### 📊 แท็บ 1 — ภาพรวมยอดขาย (ตอบข้อ 1-4)

**KPI การ์ด 4 ตัวบนสุด** (ไม่ใช่กราฟ แต่เป็นตัวเลขสรุป)
> "ดึงจาก `fact_order_items` ล้วนๆ — SUM(price) เป็นรายได้, SUM(freight_value) เป็นค่าจัดส่ง, COUNT(DISTINCT order_id) เป็นจำนวนออเดอร์ แล้วหาร revenue/orders ได้ AOV สดๆ ตรงหน้า ไม่ต้อง pre-compute ไว้ก่อน"

**กราฟรายได้รายเดือน**
- **ดึงข้อมูลจาก**: `fact_order_items` join `dim_date` ผ่าน `purchase_date_key` เอาปี-เดือนของวันสั่งซื้อ (`purchase_year_month`) มา GROUP BY แล้ว SUM(price)
- **กราฟบอกอะไร**: แต่ละแท่งคือรายได้รวมใน 1 เดือน เรียงตามเวลา
- **ใช้กราฟ**: **Bar Chart** (แกน X = เดือนแบบ Ordinal, แกน Y = รายได้) — เลือก bar เพราะข้อมูลเป็นหน่วยไม่ต่อเนื่อง (แต่ละเดือนคือก้อนแยกกัน) บาร์ทำให้เทียบความสูง/มองพีคได้ไวกว่าเส้นเมื่อจำนวนจุดไม่เยอะ (~25 เดือน)
- **ตอบคำถามข้อ**: 2 (แนวโน้มยอดขายรายเดือน) — พบพีคเดือน พ.ย. 2017 ~1 ล้านเรียล (Black Friday)

**กราฟ 10 หมวดหมู่ที่ทำรายได้สูงสุด**
- **ดึงข้อมูลจาก**: `fact_order_items` join `dim_products` เอา `category` มา GROUP BY แล้ว SUM(price), COUNT(*) เรียงมากไปน้อย LIMIT 10
- **กราฟบอกอะไร**: หมวดหมู่ไหนทำเงินให้แพลตฟอร์มมากที่สุด
- **ใช้กราฟ**: **Horizontal Bar Chart** (แกน Y = ชื่อหมวดหมู่ sort ตามรายได้) — เลือกแนวนอนเพราะชื่อหมวดหมู่แปลไทยยาว (เช่น "เฟอร์นิเจอร์และของแต่งบ้าน") ถ้าวางแนวตั้งตัวหนังสือจะทับกันอ่านไม่ออก แนวนอนให้พื้นที่เขียนชื่อเต็มบรรทัด
- **ตอบคำถามข้อ**: 1 (หมวดหมู่ไหนยอดขายรวมสูงสุด)

**กราฟรายได้ตามภูมิภาค**
- **ดึงข้อมูลจาก**: `fact_order_items` join `dim_customers` join `dim_geography` (บทบาทที่อยู่ลูกค้า) เอา `region` (1 ใน 5 ภูมิภาคทางการของบราซิล) มา SUM(price)
- **กราฟบอกอะไร**: สัดส่วนรายได้แบ่งตามภูมิภาคของลูกค้า
- **ใช้กราฟ**: **Donut Chart** (mark_arc) — เลือกวงกลมเพราะมีแค่ 5 หมวดหมู่ (ไม่เยอะเกินจนอ่านสัดส่วนไม่ออก) และคำถามคือ "สัดส่วนต่อทั้งหมด" ซึ่งวงกลมสื่อ part-of-whole ได้ตรงกว่า bar
- **ตอบคำถามข้อ**: 4 (ลูกค้ารัฐ/ภูมิภาคไหนสร้างยอดขายสูงสุด)

**กราฟราคาเฉลี่ยต่อชิ้น แยกตามหมวดหมู่**
- **ดึงข้อมูลจาก**: `fact_order_items` join `dim_products` — AVG(price) ต่อหมวดหมู่ (กรองเฉพาะหมวดที่ขายอย่างน้อย 30 ชิ้นกันหมวดเล็กเกินไปบิดเบือนค่าเฉลี่ย) เรียงแพงไปถูก 12 อันดับ
- **กราฟบอกอะไร**: หมวดไหนราคาต่อชิ้นแพง (ไม่ใช่รายได้รวม) พร้อม tooltip โชว์จำนวนที่ขายได้คู่กัน เพื่อดูว่าแพงแต่ขายน้อยหรือเปล่า
- **ใช้กราฟ**: **Horizontal Bar Chart** — เหตุผลเดียวกับกราฟหมวดหมู่ด้านบน (label ยาว)
- **ตอบคำถามข้อ**: 3 (สินค้าประเภทใดราคาเฉลี่ยต่อชิ้นสูงสุด)

---

### 👥 แท็บ 2 — ลูกค้า (ตอบข้อ 5, 7)

**กราฟซื้อครั้งเดียว vs ซื้อซ้ำ (2 กราฟคู่กัน)**
- **ดึงข้อมูลจาก**: `fact_order_items` group by `customer_unique_id` (ผ่าน `dim_customers`) นับ COUNT(DISTINCT order_id) ต่อคน แล้วจัดกลุ่มเป็น "ซื้อครั้งเดียว" กับ "ซื้อซ้ำ" จากนั้น SUM(price) แยกกลุ่ม
- **กราฟบอกอะไร**: กราฟซ้าย = จำนวนคนในแต่ละกลุ่ม, กราฟขวา = รายได้รวมของแต่ละกลุ่ม — วางคู่กันเพื่อเทียบว่าสัดส่วนคนกับสัดส่วนรายได้ต่างกันแค่ไหน (สื่อรูปแบบ 80/20)
- **ใช้กราฟ**: **Bar Chart 2 ตัวคู่กัน** — แค่ 2 หมวดหมู่ต่อกราฟ bar เรียบง่ายสุดและเทียบขนาดตรงไปตรงมาที่สุด
- **ตอบคำถามข้อ**: 5 (ลูกค้ากลับมาซื้อซ้ำกี่ % ของทั้งหมด) — พบว่าซื้อซ้ำแค่ 3.1% แต่สร้างรายได้ 5.6%

**กราฟ RFM — สัดส่วนรายได้ตามกลุ่มยอดใช้จ่าย**
- **ดึงข้อมูลจาก**: `fact_order_items` — คำนวณ Frequency (COUNT(DISTINCT order_id)) และ Monetary (SUM(price)) ต่อลูกค้า 1 คน แล้วใช้ `NTILE(5)` แบ่งลูกค้าเป็น 5 กลุ่มเท่าๆ กันตาม Monetary (กลุ่ม 5 = จ่ายเยอะสุด) จากนั้นหา % ของรายได้รวมที่แต่ละกลุ่มสร้าง
- **กราฟบอกอะไร**: กลุ่มลูกค้าไหน (แบ่งตามยอดใช้จ่าย) กินสัดส่วนรายได้เท่าไหร่
- **ใช้กราฟ**: **Bar Chart ไล่สีตามค่า (color scale)** — ใช้สีไล่โทนเข้ม-อ่อนตาม % รายได้ ช่วยให้เห็นกลุ่มเด่นได้เร็วกว่าดูตัวเลขเฉยๆ
- **ตอบคำถามข้อ**: 7 (RFM: ลูกค้ากลุ่มใดมีมูลค่าและความถี่สูงสุด) — พบว่ากลุ่มบนสุด 20% สร้างรายได้ ~57%

---

### 🚚 แท็บ 3 — การจัดส่ง (ตอบข้อ 9, 10, 13)

**KPI การ์ดเวลาจัดส่ง** (ไม่ใช่กราฟ)
> "ดึงจาก `fact_order_items` — AVG(delivery_days), AVG(seller_processing_days), AVG(carrier_transit_days), AVG(is_late_delivery)*100 ทั้งหมดเป็น measure ที่คำนวณไว้แล้วตอนสร้าง fact จากผลต่างของ timestamp ทั้ง 5 จุดใน `dim_date`"

**กราฟเวลาที่ผู้ขายใช้เตรียมของ แยกตามรัฐผู้ขาย**
- **ดึงข้อมูลจาก**: `fact_order_items` join `dim_sellers` เอา `state` ของผู้ขาย มา AVG(seller_processing_days) (กรองเฉพาะรัฐที่ส่งของอย่างน้อย 50 ชิ้น กันรัฐที่มีข้อมูลน้อยเกินจนค่าเฉลี่ยแกว่ง)
- **กราฟบอกอะไร**: รัฐไหนที่ผู้ขายเตรียมของช้า/เร็ว
- **ใช้กราฟ**: **Bar Chart เรียงจากมากไปน้อย** — เหมาะกับการจัดอันดับ (ranking) ระหว่างรัฐ
- **ตอบคำถามข้อ**: 9 (ผู้ขายรัฐไหนเตรียมสินค้าเฉลี่ยนานสุด)

**กราฟอัตราส่งช้ากว่ากำหนด แยกตามภูมิภาค**
- **ดึงข้อมูลจาก**: `fact_order_items` join `dim_geography` (บทบาทที่อยู่ลูกค้า) — AVG(is_late_delivery)*100 ต่อภูมิภาค
- **กราฟบอกอะไร**: ภูมิภาคไหนโดนส่งช้าบ่อยกว่า
- **ใช้กราฟ**: **Bar Chart** เรียงมากไปน้อย — จัดอันดับ 5 ภูมิภาคเทียบกัน
- **ตอบคำถามข้อ**: 10 (ระยะเวลาขนส่งและความล่าช้าแตกต่างกันอย่างไร)

**กราฟ "ส่งช้าทำให้คะแนนรีวิวแย่ลงไหม" ⭐ (Drill-Across)**
- **ดึงข้อมูลจาก**: **2 Fact** — `fact_order_items` (is_late_delivery, delivery_delay_days) จัดกลุ่มความช้าเป็น 4 ระดับ ไป join กับ `fact_order_reviews` (review_score) ผ่าน `order_id` ที่เป็น key ร่วมกัน (นี่คือ Drill-Across ตัวจริง ไม่ใช่แค่ join ธรรมดาเพราะเป็นคนละ Fact คนละ grain)
- **กราฟบอกอะไร**: แท่ง = จำนวนออเดอร์ในแต่ละระดับความช้า (context ว่ากลุ่มไหนมีตัวอย่างเยอะ/น้อย), เส้น = คะแนนรีวิวเฉลี่ยของกลุ่มนั้น
- **ใช้กราฟ**: **Combo Chart (Bar + Line ซ้อนกัน, แกน Y คนละแกน)** — เลือกผสม 2 แบบเพราะต้องโชว์ 2 มิติพร้อมกัน: ปริมาณข้อมูล (แท่ง, แกนซ้าย) กับแนวโน้มคะแนน (เส้น, แกนขวา) ถ้าใช้ bar อย่างเดียวจะมองไม่เห็นทิศทางแนวโน้มชัดเท่าเส้น
- **ตอบคำถามข้อ**: 13 (คะแนนรีวิวแตกต่างกันอย่างไรตามปัจจัยการจัดส่ง) — พบว่าตรงเวลา 4.29 ดาว vs ช้า 2.57 ดาว ต่างกัน 1.72 ดาว — **นี่คือกราฟที่พิสูจน์ว่าทำไมต้องมี Galaxy Schema เพราะต้องเชื่อม 2 Fact เข้าด้วยกัน**

---

### 💳 แท็บ 4 — การชำระเงิน (ตอบข้อ 6, 8)

**กราฟลูกค้าจ่ายเงินด้วยวิธีใด (2 กราฟคู่กัน)**
- **ดึงข้อมูลจาก**: `fact_order_payments` join `dim_payment_type` — COUNT(DISTINCT order_id) และ AVG(payment_value) ต่อวิธีชำระเงิน
- **กราฟบอกอะไร**: ซ้าย = ใช้วิธีไหนบ่อยสุด, ขวา = วิธีไหนมูลค่าต่อรายการสูงสุด
- **ใช้กราฟ**: **Horizontal Bar Chart** — มีแค่ 4 วิธีชำระเงิน จัดอันดับง่ายด้วย bar แนวนอน
- **ตอบคำถามข้อ**: 8 (วิธีชำระเงินใดใช้มากสุด มูลค่าเฉลี่ยเท่าไหร่)

**กราฟงวดผ่อน → ยอดจ่ายเฉลี่ย**
- **ดึงข้อมูลจาก**: `fact_order_payments` — จัดกลุ่ม `payment_installments` เป็นช่วง (1, 2-3, 4-6, 7-12, 13+) แล้ว AVG(payment_value) ต่อช่วง
- **กราฟบอกอะไร**: ยิ่งผ่อนหลายงวด ยอดที่จ่ายเฉลี่ยสูงขึ้นไหม
- **ใช้กราฟ**: **Bar Chart** เรียงตามจำนวนงวดจากน้อยไปมาก — ใช้ bar แทน line เพราะแกน X เป็นช่วง (bin) ไม่ใช่ตัวเลขต่อเนื่องจริงๆ
- **ตอบคำถามข้อ**: 6 บางส่วน (พฤติกรรมการผ่อนชำระ)

**กราฟ "มูลค่าตะกร้า vs ยอดจ่ายจริง" ⭐ (Drill-Across)**
- **ดึงข้อมูลจาก**: **2 Fact** — `fact_order_items` (SUM(total_item_value) = มูลค่าตะกร้าที่สั่ง) join `fact_order_payments` (SUM(payment_value) = ยอดที่จ่ายจริง) ผ่าน `order_id` ร่วมกัน แล้วแบ่งกลุ่มตามจำนวนงวดผ่อน
- **กราฟบอกอะไร**: เทียบมูลค่าสินค้าที่สั่งกับยอดที่จ่ายจริงในแต่ละกลุ่มงวดผ่อน ช่องว่างที่ถ่างขึ้นคือดอกเบี้ย
- **ใช้กราฟ**: **Grouped/Dodged Bar Chart** (2 แท่งเคียงกันต่อ 1 กลุ่มงวดผ่อน) — เลือกวางเคียงกันแทนซ้อนหรือเส้น เพราะต้องการให้เทียบค่าคู่ (ตะกร้า vs จ่ายจริง) ในแต่ละกลุ่มได้ตรงๆ ด้วยสายตา
- **ตอบคำถามข้อ**: 6 (ยอดจ่ายจริงเทียบกับมูลค่าตะกร้า) — พิสูจน์ Galaxy Schema อีกจุดเพราะข้าม 2 Fact

---

### ⭐ แท็บ 5 — คุณภาพและรีวิว (ตอบข้อ 11, 12, 13)

**กราฟการกระจายของคะแนนรีวิว**
- **ดึงข้อมูลจาก**: `fact_order_reviews` — COUNT(*) ต่อ `review_score` (1-5)
- **กราฟบอกอะไร**: ลูกค้าให้กี่ดาวบ่อยที่สุด เป็น histogram พื้นฐานก่อนเจาะกราฟถัดๆ ไป
- **ใช้กราฟ**: **Bar Chart ไล่สีแดง-เหลือง-เขียว** ตามคะแนน — สีช่วยสื่อความหมาย "แย่→ดี" ได้ทันทีโดยไม่ต้องอ่านตัวเลข
- **ตอบคำถามข้อ**: บริบทของข้อ 12

**กราฟคะแนนรีวิวเฉลี่ย แยกตามหมวดหมู่สินค้า ⭐ (Drill-Across)**
- **ดึงข้อมูลจาก**: **2 Fact** — หาหมวดหมู่หลักของแต่ละออเดอร์จาก `fact_order_items` join `dim_products` (ถ้าออเดอร์มีหลายหมวด เลือกหมวดที่มีจำนวนชิ้นเยอะสุดด้วย ROW_NUMBER) แล้ว join กับคะแนนเฉลี่ยจาก `fact_order_reviews` ผ่าน `order_id` (กรองเฉพาะหมวดที่มีรีวิวอย่างน้อย 50 ออเดอร์)
- **กราฟบอกอะไร**: โชว์ 8 หมวดดีสุด + 8 หมวดแย่สุด เรียงจากบนลงล่าง
- **ใช้กราฟ**: **Horizontal Bar Chart ไล่สีตามคะแนน** — แนวนอนเพราะชื่อหมวดหมู่ยาว ไล่สีช่วยแยกโซนดี/แย่ได้ไว
- **ตอบคำถามข้อ**: 12 (หมวดหมู่ใดคะแนนรีวิวสูง/ต่ำสุด)

**กราฟค่าจัดส่งคิดเป็นสัดส่วนของราคาสินค้า แยกตามหมวดหมู่**
- **ดึงข้อมูลจาก**: `fact_order_items` join `dim_products` — SUM(freight_value)/SUM(price)*100 ต่อหมวดหมู่ (กรองอย่างน้อย 50 ชิ้น)
- **กราฟบอกอะไร**: หมวดไหนค่าส่งแพงเทียบกับราคาสินค้า (มักเป็นของใหญ่/หนักแต่ราคาไม่สูง)
- **ใช้กราฟ**: **Horizontal Bar Chart** จัดอันดับ 12 หมวดแรก
- **ตอบคำถามข้อ**: 11 (หมวดหมู่ใดสัดส่วนค่าจัดส่งต่อราคาสูงสุด)

**กราฟ "ระยะทางผู้ซื้อ-ผู้ขาย → คะแนนรีวิว" ⭐ (Drill-Across)**
- **ดึงข้อมูลจาก**: **2 Fact** — `fact_order_items.buyer_seller_distance_km` (คำนวณจากพิกัด 2 บทบาทของ `dim_geography` คือที่อยู่ลูกค้ากับที่อยู่ผู้ขาย) จัดกลุ่มเป็นช่วงระยะทาง แล้ว join คะแนนจาก `fact_order_reviews` ผ่าน `order_id`
- **กราฟบอกอะไร**: แท่ง = จำนวนออเดอร์ในแต่ละช่วงระยะทาง, เส้น = คะแนนรีวิวเฉลี่ย
- **ใช้กราฟ**: **Combo Chart (Bar + Line, แกน Y คนละแกน)** เหตุผลเดียวกับกราฟส่งช้าในแท็บการจัดส่ง — ต้องการเห็นทั้งปริมาณและแนวโน้มพร้อมกัน
- **ตอบคำถามข้อ**: 13 อีกมุม — พบว่าระยะทางกระทบแค่ ~-0.2 ดาว เทียบกับความล่าช้าที่กระทบ -1.72 ดาว สรุปได้ว่า **ความตรงเวลาสำคัญกว่าระยะทางมาก**

---

### 🏪 แท็บ 6 — ผู้ขายและตะกร้า (ตอบข้อ 14, 15)

**กราฟ Pareto 80/20**
- **ดึงข้อมูลจาก**: `fact_order_items` group by `seller_key` (ผ่าน `dim_sellers`) — SUM(price) ต่อผู้ขาย แล้วใช้ Window Function (`ROW_NUMBER`, running SUM) เรียงผู้ขายจากรายได้มากไปน้อย คำนวณว่าผู้ขาย X% แรกสะสมรายได้ได้กี่ % ของทั้งหมด (ตัดที่ 1%, 5%, 10%, 20%)
- **กราฟบอกอะไร**: ยิ่งกลุ่มผู้ขายเล็กแต่กินสัดส่วนรายได้เยอะ ยิ่งแปลว่าแพลตฟอร์มพึ่งพาผู้ขายรายใหญ่ไม่กี่ราย
- **ใช้กราฟ**: **Bar Chart ไล่สีส้มตามค่า** — สรุปเป็น 4 บักเก็ต (1/5/10/20%) แทนเส้นโค้ง Pareto เต็มรูปแบบ เพื่อให้อ่านตัวเลขตรงจุดสำคัญได้ไวในที่ประชุม
- **ตอบคำถามข้อ**: 14 (ผู้ขาย Top 10% สร้างยอดขายกี่ % ของทั้งหมด) — พบว่า ~68%

**กราฟ Market Basket — หมวดหมู่ที่ถูกซื้อร่วมกันบ่อย**
- **ดึงข้อมูลจาก**: `fact_order_items` join `dim_products` แล้ว **self-join** ตารางเดียวกันบน `order_id` เดียวกัน (เงื่อนไข `a.category < b.category` กันนับคู่ซ้ำ) นับ COUNT(*) ว่าคู่หมวดหมู่ไหนอยู่ในออเดอร์เดียวกันบ่อยสุด
- **กราฟบอกอะไร**: คู่สินค้าที่มักถูกซื้อพร้อมกัน เป็นจุดตั้งต้นทำ cross-sell/bundle
- **ใช้กราฟ**: **Horizontal Bar Chart** จัดอันดับ 12 คู่แรก — label เป็นชื่อคู่หมวดหมู่ที่ยาว ใช้แนวนอนอ่านง่ายกว่า
- **ตอบคำถามข้อ**: 15 (สินค้าคู่ใดถูกซื้อร่วมกันบ่อยที่สุด)

### [สรุปปิดการพรีเซนต์ — ต่อด้วยสไลด์ 16 "That's a wrap"]

> "จะเห็นได้ว่า Dashboard นี้ไม่ได้แค่โชว์ตัวเลข แต่ตอบครบทั้ง 15 Business Questions ด้วยข้อมูลจริง 100% ผ่านโครงสร้าง Fact Constellation ที่จำเป็นจริงและพิสูจน์ได้ด้วย Drill-Across 3 จุด พร้อม dbt test ผ่านเต็ม 100% ยืนยันคุณภาพข้อมูล
>
> พวกเราขอจบการนำเสนอเพียงเท่านี้ ขอบพระคุณอาจารย์และเพื่อนๆ ทุกท่านครับ/ค่ะ" *(สลับไปสไลด์ 16)*
